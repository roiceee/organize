interface TaskCalendarToggleContextInterface {
  showCalendarState: boolean;
  setShowCalendarState: React.Dispatch<React.SetStateAction<boolean>>;
}

export default TaskCalendarToggleContextInterface;
